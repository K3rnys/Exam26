[← Назад](../../)

# 8. Перебор слов и системы счисления

[1 часть](#Подсчет%20количества%20разных%20последовательностей), [2 часть](#Подсчет%20количества%20слов%20с%20ограничениями), [3 часть](#Слова%20по%20порядку)

Задание проверяет твоё понимание комбинаторики, как в ЕГЭ по математике, но по сути всё сводится к библиотеке `itertools`, а точнее даже к 3ем её функциям, которые избавят нас от необходимости считать руками:

- `product` → все слова с повторениями
- `permutations` → перестановки без повторений
- `combinations` → уникальные комбинации без учета порядка в них

Импортировать их можно так:

```python
from itertools import product
from itertools import permutations
from itertools import combinations
```
 
Или просто `from itertools import *`, если хочется всё сразу..

**Product**

Обычно `product` используется в задачах чаще всего, можешь ориентироваться на подсказку условия "*буквы могут повторяться*"

Например:
```python
from itertools import product

for p in product('ABC', repeat=2):  
print(''.join(p))
```

Выдаст нам все сочетания двух букв из строки `'ABC'`:

```python
AA  
AB  
AC  
BA  
BB  
BC  
CA  
CB  
CC
```

> P.S `product `- это итератор, такие штуки порождают итерируемые объекты вроде `range(a, b)`, но забивать этим голову не надо, о них мы узнаем лучше потом
>
> Здесь `p` тоже является итерируемым объектом и выглядит как кортеж из символов `('A', 'A')`, в следующем шаге `('A', 'B')` и т.д., для чего мы используем строковый метод `.join()`, который к строке присоединяет всё из другой строки, или даже коллекции, которая состоит из строк (для словарей по умолчанию присоединяет ключи, но можно в цикле доставать и значения `''.join(str(v) for v in d.values())`)


**Permutations**

Отличие от первого только в том, что буквы повторяться не будут:

```python
from itertools import permutations

for p in permutations("ABC", 2):  
print(''.join(p))

AB  
AC  
BA  
BC  
CA  
CB
```
> P.S кстати `permutations` и `combinations` - тоже итераторы)


**Сombinations**

Если до этого мы учитывали комбинации `AB` и `BA` за две, то здесь это будет считаться за одну, ведь порядок совсем не важен

```python
from itertools import combinations  
  
print(list(combinations("ABC", 2)))

[('A', 'B'), ('A', 'C'), ('B', 'C')]
```


---

## Подсчет количества разных последовательностей

[Решу ЕГЭ, 25 шт.](https://inf-ege.sdamgia.ru/test?theme=395)

Сразу хочу обратить внимание на подобные задания

*Тип 8 № [58235](https://inf-ege.sdamgia.ru/problem?id=58235)*

![](Pasted%20image%2020260424035814.png)

Первой моей мыслью было, что n-значные числа переводят из десятичной записи в другую, но это не так, в условии подразумевают, что это n-значное число в уже имеющейся (четвертичной) системе счисления

Решение выглядит довольно просто

![](Pasted%20image%2020260424035128.png)

```python
from itertools import product
alphabet = '0123'
count = 0
for i in product(alphabet, repeat=3):
	if (i[0] != '0') and (int(i[0]) + int(i[2]) > int(i[1])):
		count += 1
print(count)
>
38
```
  Обязательно смотри, что ты не начинаешь с 0, и проводишь мат. операции с `int`, а не `str`, как я)

> Вот, попробуй
> 
> *Тип 8 № [58237](https://inf-ege.sdamgia.ru/problem?id=58237)*
> 
> ![](Pasted%20image%2020260424040003.png)

<details>  
<summary><b>Ответ</b></summary>  
  40
</details>


*Тип 8 № [59743](https://inf-ege.sdamgia.ru/problem?id=59743)*

![](Pasted%20image%2020260503002510.png)

Аналогично, просто меняем условие под требования в задании

![](Pasted%20image%2020260503003412.png)

```python
from itertools import product
alphabet = 'МАНГУСТ'
count = 0
for i in product(alphabet, repeat=6):
	if (i[0] != 'А') and (i.count('У') >=1 ) and (i.count('М') == 2):
		count += 1
print(count)
>
9155
```


> Это на подумать
> 
> *Тип 8 № [73863](https://inf-ege.sdamgia.ru/problem?id=73863)*
> 
> ![](Pasted%20image%2020260503003851.png)

<details>  
<summary><b>Ответ</b></summary>  
  73206121
</details>


> Вот тут интересные задания, я попрошу тебя для первого пояснить готовое решение, а для второго сделать его самой, написать руками без копирования, удачи
>
> *Тип 8 № [87399](https://inf-ege.sdamgia.ru/problem?id=87399)*
> 
> *Тип 8 № [87426](https://inf-ege.sdamgia.ru/problem?id=87426)*
> 
> ![](Pasted%20image%2020260503005238.png)
> 
> P.s Сделаю небольшую подсказку, `[]` - это список, понятно, но в его краткой записи внутри мы можем сразу указать, чем хотим его наполнить
> 
> Например, `cet = [x for x in range(1,10) if x > 5]` даст нам список, содержащий `[6, 7, 8, 9]`, или `[x if x > 5 else x + 5 for x in range(1,10)]` выдаст `[6, 7, 8, 9, 10, 6, 7, 8, 9]`
> 
> Так, строка `cet = [x for x in i if x in '02468a']` заполняет `cet` только четными цифрами из числа `i`, то есть записывает в список наш `x` только в том случае, если `x` есть в строке `'02468a'`, при этом `i` - кортеж, и `x` как раз перебирает элементы этого кортежа
> 
>  Иначе говоря, выглядит это так:
```python
for x in i:  
	if x in '02468a':  
		cet.append(x)
```

> А теперь добавь комментарии к коду, и скинь мне:
>
```python
from itertools import product
otv = 0
for i in product('0123456789ab', repeat=5):
	if i[0] != '0':
		cet = [x for x in i if x in '02468a']
		if len(set(cet)) == 1 and len(cet) == 3:
			if cet[0]*3 in ''.join(i):
				otv += 1
print(otv)
```

> И попробуй самостоятельно решить второе

<details>  
<summary><b>К слову, ответ в нем:</b></summary>  
  576
</details>

> Не стесняйся, реши еще это, и на этом скажем, что ты овладела всеми заданиями из этой подтемы
> 
> Решения обязательно закидывай мне на проверку, может подскажу где пару финтов ушами
> 
> P.s Мем, это задание вообще из последней подтемы, закинутое сюда разрабом сайта по ошибке..
> 
> Тип 8 № [89745](https://inf-ege.sdamgia.ru/problem?id=89745)
> 
> ![](Pasted%20image%2020260503013222.png)


<details>  
<summary><b>Ответ:</b></summary>  
  7903
</details>


---

## Подсчет количества слов с ограничениями

[Решу ЕГЭ, 46 шт.](https://inf-ege.sdamgia.ru/test?theme=379)

> Сначала реши задание, которое не сильно то отличается от предыдущей подтемы, можешь сделать это устно, а потом попробуй сделать это кодом в 2 строки с учетом импорта)
> 
> Я ведь не зря распинался про краткую запись в списки, используй это
> 
> P.s Тебе не обязательно где то хранить слова, ведь обычное условие в начале записи, например, `[x>5 if.. for..]` будет создавать список из элементов `True` и `False`, а их количество в списке можно подсчитать с помощью `.count()`
> 
> Если не получается, сделай рабочий код так, как можешь, и затем напиши мне, я помогу преобразовать
> 
> ![](Pasted%20image%2020260503013816.png)

<details>  
<summary><b>Ответ:</b></summary>  
  162
</details>

> На ЕГЭ тебе точно не следует делать сложные условия внутри `[]`, чтобы не запутаться, но порой такая запись помогает сэкономить немного времени, время от времени мы будем её использовать


*Тип 8 № [27268](https://inf-ege.sdamgia.ru/problem?id=27268)*

![](Pasted%20image%2020260503020820.png)

![](Pasted%20image%2020260503021400.png)

``` python
from itertools import permutations
count = 0
for i in permutations('РУСЛАН', 6):
	if 'УА' not in ''.join(i) and 'АУ' not in ''.join(i):
		count += 1
print(count)
```

> Вот, попробуй
> 
> Тип 8 № [35982](https://inf-ege.sdamgia.ru/problem?id=35982)
> 
> ![](Pasted%20image%2020260503023653.png)

<details>  
<summary><b>Ответ:</b></summary>  
  6075
</details>


> Подумай, в этой ситуации лучше использовать `product` или `permutations`, и не забудь про `set()` 
> 
> Тип 8 № [40724](https://inf-ege.sdamgia.ru/problem?id=40724)
> 
> ![](Pasted%20image%2020260503023326.png)

<details>  
<summary><b>Ответ:</b></summary>  
  15120
</details>


---

## Слова по порядку

[Решу ЕГЭ, 58 шт.](https://inf-ege.sdamgia.ru/test?theme=243)

В этом типе нет ничего нового


> Все задания здесь похожие, и этого будет достаточно
> 
> Тип 8 № [45242](https://inf-ege.sdamgia.ru/problem?id=45242)
> 
> ![](Pasted%20image%2020260503025058.png)

<details>  
<summary><b>Ответ:</b></summary>  
  131
</details>

